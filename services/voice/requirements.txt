# Mission Clawtrol Voice Sidecar — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework + WebSocket server
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# Speech-to-Text (CTranslate2-based Whisper, CPU-optimized)
faster-whisper>=1.0.0

# Text-to-Speech (fast local neural TTS)
piper-tts>=1.0.0

# HTTP client (for calling MC backend)
httpx>=0.24.0

# NumPy (used by faster-whisper and piper internally)
numpy>=1.24.0

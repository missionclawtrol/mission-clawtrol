# ============================================
# Mission Clawtrol — Environment Configuration
# ============================================
# Copy to .env and fill in values:
#   cp .env.example .env

# --- Required ---

# GitHub OAuth App credentials (create at https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Session signing secret (generate: openssl rand -hex 32)
SESSION_SECRET=

# PostgreSQL password
POSTGRES_PASSWORD=

# --- Optional ---

# OpenClaw Gateway connection (for approval queue + agent monitoring)
# Default: ws://host.docker.internal:18789 (reaches host machine from Docker)
GATEWAY_URL=ws://host.docker.internal:18789
GATEWAY_TOKEN=

# Restrict login to members of a specific GitHub organization
# Leave empty to allow any GitHub user
ALLOWED_ORG=

# Public URLs (used for OAuth redirects and API connections)
PUBLIC_URL=http://localhost:5173
PUBLIC_API_URL=http://localhost:3001
